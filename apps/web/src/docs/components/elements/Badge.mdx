import { Badge, Button } from '@losensky-systems/web-components-core'
import { ShoppingCart, Bell, Heart, MessageCircle, Settings, User } from 'lucide-react'

# Badge Component

The Badge component displays a notification count or indicator that overlays any element. Perfect for shopping carts, notifications, messages, and more.

## Import

```tsx
import { Badge } from '@losensky-systems/web-components-core'
```

## Basic Usage

Wrap any element with a Badge to show a count or text:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={2}>
    <Button icon={<ShoppingCart size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={5}>
    <Button icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={12}>
    <Button icon={<MessageCircle size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={0} showZero>
    <Button icon={<Heart size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge text="New">
    <Button size="sm">Product</Button>
  </Badge>
  
  <Badge text="Sale" variant="error">
    <Button size="sm" variant="outline">Item</Button>
  </Badge>
</div>

```tsx
<Badge count={2}>
  <Button icon={<ShoppingCart size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={5}>
  <Button icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={12}>
  <Button icon={<MessageCircle size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={0} showZero>
  <Button icon={<Heart size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge text="New">
  <Button size="sm">Product</Button>
</Badge>

<Badge text="Sale" variant="error">
  <Button size="sm" variant="outline">Item</Button>
</Badge>
```

## Text Badges

Display text content instead of numbers:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge text="New">
    <Button size="sm">Product</Button>
  </Badge>
  
  <Badge text="Sale" variant="error">
    <Button size="sm" variant="outline">Item</Button>
  </Badge>
  
  <Badge text="Featured" variant="success">
    <Button size="sm" variant="outline">Article</Button>
  </Badge>
  
  <Badge text="Hot" variant="warning">
    <Button size="sm" variant="outline">Trending</Button>
  </Badge>
  
  <Badge text="Beta" variant="info">
    <Button size="sm" variant="outline">Feature</Button>
  </Badge>
</div>

```tsx
<Badge text="New">
  <Button size="sm">Product</Button>
</Badge>

<Badge text="Sale" variant="error">
  <Button size="sm" variant="outline">Item</Button>
</Badge>

<Badge text="Featured" variant="success">
  <Button size="sm" variant="outline">Article</Button>
</Badge>

<Badge text="Hot" variant="warning">
  <Button size="sm" variant="outline">Trending</Button>
</Badge>

<Badge text="Beta" variant="info">
  <Button size="sm" variant="outline">Feature</Button>
</Badge>
```

### Text Badge Sizes

Text badges scale with different sizes:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge text="New" size="sm">
    <Button size="sm">Small</Button>
  </Badge>
  
  <Badge text="Sale" size="md">
    <Button size="md">Medium</Button>
  </Badge>
  
  <Badge text="Featured" size="lg">
    <Button size="lg">Large</Button>
  </Badge>
</div>

```tsx
<Badge text="New" size="sm">
  <Button size="sm">Small</Button>
</Badge>

<Badge text="Sale" size="md">
  <Button size="md">Medium</Button>
</Badge>

<Badge text="Featured" size="lg">
  <Button size="lg">Large</Button>
</Badge>
```

## Dot Indicator

Use a simple dot instead of a number:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge dot>
    <Button icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge dot variant="success">
    <Button icon={<Settings size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge dot variant="warning">
    <Button icon={<User size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge dot variant="error">
    <Button icon={<Heart size={16} />} iconOnly variant="ghost" />
  </Badge>
</div>

```tsx
<Badge dot>
  <Button icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge dot variant="success">
  <Button icon={<Settings size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge dot variant="warning">
  <Button icon={<User size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge dot variant="error">
  <Button icon={<Heart size={16} />} iconOnly variant="ghost" />
</Badge>
```

## Variants

Different color variants for different contexts:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={3} variant="default">
    <Button size="sm">Default</Button>
  </Badge>
  
  <Badge count={5} variant="success">
    <Button size="sm" variant="outline">Success</Button>
  </Badge>
  
  <Badge count={2} variant="warning">
    <Button size="sm" variant="outline">Warning</Button>
  </Badge>
  
  <Badge count={7} variant="error">
    <Button size="sm" variant="outline">Error</Button>
  </Badge>
  
  <Badge count={1} variant="info">
    <Button size="sm" variant="outline">Info</Button>
  </Badge>
</div>

```tsx
<Badge count={3} variant="default">
  <Button size="sm">Default</Button>
</Badge>

<Badge count={5} variant="success">
  <Button size="sm" variant="outline">Success</Button>
</Badge>

<Badge count={2} variant="warning">
  <Button size="sm" variant="outline">Warning</Button>
</Badge>

<Badge count={7} variant="error">
  <Button size="sm" variant="outline">Error</Button>
</Badge>

<Badge count={1} variant="info">
  <Button size="sm" variant="outline">Info</Button>
</Badge>
```

## Sizes

Control the badge size:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={3} size="sm">
    <Button size="sm" icon={<Bell size={14} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={5} size="md">
    <Button size="md" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={2} size="lg">
    <Button size="lg" icon={<Bell size={18} />} iconOnly variant="ghost" />
  </Badge>
</div>

```tsx
<Badge count={3} size="sm">
  <Button size="sm" icon={<Bell size={14} />} iconOnly variant="ghost" />
</Badge>

<Badge count={5} size="md">
  <Button size="md" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={2} size="lg">
  <Button size="lg" icon={<Bell size={18} />} iconOnly variant="ghost" />
</Badge>
```

## Position

Control where the badge appears:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={3} position="top-left">
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={5} position="top-right">
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={2} position="bottom-left">
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={7} position="bottom-right">
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
</div>

```tsx
<Badge count={3} position="top-left">
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={5} position="top-right">
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={2} position="bottom-left">
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={7} position="bottom-right">
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>
```

## Max Count

Limit the maximum number displayed:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={5} max={5}>
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={150} max={99}>
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={1000} max={9}>
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
</div>

```tsx
<Badge count={5} max={5}>
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={150} max={99}>
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={1000} max={9}>
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>
```

## Custom Offset

Fine-tune badge positioning:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={3} offset={{ x: 5, y: -5 }}>
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={5} offset={{ x: -5, y: 5 }}>
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={2} offset={{ x: 0, y: -10 }}>
    <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
</div>

```tsx
<Badge count={3} offset={{ x: 5, y: -5 }}>
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={5} offset={{ x: -5, y: 5 }}>
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

<Badge count={2} offset={{ x: 0, y: -10 }}>
  <Button size="sm" icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>
```

## Real-World Examples

Common use cases for badges:

<div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap', alignItems: 'center' }}>
  <Badge count={3} variant="error">
    <Button icon={<ShoppingCart size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={12} variant="info">
    <Button icon={<MessageCircle size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge dot variant="success">
    <Button icon={<Settings size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge count={99} max={99} variant="warning">
    <Button icon={<Bell size={16} />} iconOnly variant="ghost" />
  </Badge>
  
  <Badge text="New" variant="success">
    <Button size="sm">Product</Button>
  </Badge>
  
  <Badge text="Sale" variant="error">
    <Button size="sm" variant="outline">Item</Button>
  </Badge>
  
  <Badge text="Beta" variant="info">
    <Button size="sm" variant="ghost">Feature</Button>
  </Badge>
</div>

```tsx
// Shopping cart with items
<Badge count={3} variant="error">
  <Button icon={<ShoppingCart size={16} />} iconOnly variant="ghost" />
</Badge>

// Messages with count
<Badge count={12} variant="info">
  <Button icon={<MessageCircle size={16} />} iconOnly variant="ghost" />
</Badge>

// Settings with notification dot
<Badge dot variant="success">
  <Button icon={<Settings size={16} />} iconOnly variant="ghost" />
</Badge>

// Notifications with max limit
<Badge count={99} max={99} variant="warning">
  <Button icon={<Bell size={16} />} iconOnly variant="ghost" />
</Badge>

// Product with "New" badge
<Badge text="New" variant="success">
  <Button size="sm">Product</Button>
</Badge>

// Item with "Sale" badge
<Badge text="Sale" variant="error">
  <Button size="sm" variant="outline">Item</Button>
</Badge>

// Feature with "Beta" badge
<Badge text="Beta" variant="info">
  <Button size="sm" variant="ghost">Feature</Button>
</Badge>
```

## Accessibility

The Badge component includes proper accessibility features:

- **ARIA attributes**: `role="status"` and `aria-label`
- **Screen reader friendly**: Announces count or notification status
- **Keyboard accessible**: Works with focusable elements
- **Semantic meaning**: Clear indication of notification state

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `React.ReactNode` | - | Element to overlay with badge |
| `count` | `number` | - | Number to display in badge |
| `text` | `string` | - | Text to display in badge |
| `max` | `number` | `99` | Maximum number before showing "+" |
| `showZero` | `boolean` | `false` | Show badge when count is 0 |
| `variant` | `'default' \| 'success' \| 'warning' \| 'error' \| 'info'` | `'default'` | Badge color variant |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Badge size |
| `position` | `'top-right' \| 'top-left' \| 'bottom-right' \| 'bottom-left'` | `'top-right'` | Badge position |
| `className` | `string` | `''` | Additional CSS classes |
| `dot` | `boolean` | `false` | Show as dot instead of number |
| `offset` | `{ x?: number; y?: number }` | `{ x: 0, y: 0 }` | Custom position offset |
