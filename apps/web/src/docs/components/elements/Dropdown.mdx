# Dropdown

A simple dropdown menu component for displaying a list of actions or options.

## Basic Usage

<CodeBlock code={`<Dropdown 
  items={[
    { children: 'Edit', onClick: () => console.log('Edit clicked') },
    { children: 'Delete', onClick: () => console.log('Delete clicked') },
    { children: 'Share', onClick: () => console.log('Share clicked') }
  ]}
>
  <Button>Actions</Button>
</Dropdown>`}>
  <Dropdown 
    items={[
      { children: 'Edit', onClick: () => console.log('Edit clicked') },
      { children: 'Delete', onClick: () => console.log('Delete clicked') },
      { children: 'Share', onClick: () => console.log('Share clicked') }
    ]}
  >
    <Button>Actions</Button>
  </Dropdown>
</CodeBlock>

## With Icons

<CodeBlock code={`<Dropdown 
  items={[
    { children: 'Edit', icon: <Icon name="settings" size="sm" />, onClick: () => console.log('Edit') },
    { children: 'Delete', icon: <Icon name="close" size="sm" />, onClick: () => console.log('Delete') },
    { children: 'Share', icon: <Icon name="heart" size="sm" />, onClick: () => console.log('Share') }
  ]}
>
  <Button variant="outline">Menu</Button>
</Dropdown>`}>
  <Dropdown 
    items={[
      { children: 'Edit', icon: <Icon name="settings" size="sm" />, onClick: () => console.log('Edit') },
      { children: 'Delete', icon: <Icon name="close" size="sm" />, onClick: () => console.log('Delete') },
      { children: 'Share', icon: <Icon name="heart" size="sm" />, onClick: () => console.log('Share') }
    ]}
  >
    <Button variant="outline">Menu</Button>
  </Dropdown>
</CodeBlock>

## Different Placements

<CodeBlock code={`<div className="flex gap-4">
  <Dropdown 
    placement="top"
    items={[
      { children: 'Top', onClick: () => console.log('Top') }
    ]}
  >
    <Button size="sm">Top</Button>
  </Dropdown>
  
  <Dropdown 
    placement="bottom"
    items={[
      { children: 'Bottom', onClick: () => console.log('Bottom') }
    ]}
  >
    <Button size="sm">Bottom</Button>
  </Dropdown>
  
  <Dropdown 
    placement="left"
    items={[
      { children: 'Left', onClick: () => console.log('Left') }
    ]}
  >
    <Button size="sm">Left</Button>
  </Dropdown>
  
  <Dropdown 
    placement="right"
    items={[
      { children: 'Right', onClick: () => console.log('Right') }
    ]}
  >
    <Button size="sm">Right</Button>
  </Dropdown>
</div>`}>
  <div className="flex gap-4">
    <Dropdown 
      placement="top"
      items={[
        { children: 'Top', onClick: () => console.log('Top') }
      ]}
    >
      <Button size="sm">Top</Button>
    </Dropdown>
    
    <Dropdown 
      placement="bottom"
      items={[
        { children: 'Bottom', onClick: () => console.log('Bottom') }
      ]}
    >
      <Button size="sm">Bottom</Button>
    </Dropdown>
    
    <Dropdown 
      placement="left"
      items={[
        { children: 'Left', onClick: () => console.log('Left') }
      ]}
    >
      <Button size="sm">Left</Button>
    </Dropdown>
    
    <Dropdown 
      placement="right"
      items={[
        { children: 'Right', onClick: () => console.log('Right') }
      ]}
    >
      <Button size="sm">Right</Button>
    </Dropdown>
  </div>
</CodeBlock>

## Sizes

<CodeBlock code={`<div className="flex gap-4">
  <Dropdown 
    size="sm"
    items={[
      { children: 'Small', onClick: () => console.log('Small') }
    ]}
  >
    <Button size="sm">Small</Button>
  </Dropdown>
  
  <Dropdown 
    size="md"
    items={[
      { children: 'Medium', onClick: () => console.log('Medium') }
    ]}
  >
    <Button size="sm">Medium</Button>
  </Dropdown>
  
  <Dropdown 
    size="lg"
    items={[
      { children: 'Large', onClick: () => console.log('Large') }
    ]}
  >
    <Button size="sm">Large</Button>
  </Dropdown>
</div>`}>
  <div className="flex gap-4">
    <Dropdown 
      size="sm"
      items={[
        { children: 'Small', onClick: () => console.log('Small') }
      ]}
    >
      <Button size="sm">Small</Button>
    </Dropdown>
    
    <Dropdown 
      size="md"
      items={[
        { children: 'Medium', onClick: () => console.log('Medium') }
      ]}
    >
      <Button size="sm">Medium</Button>
    </Dropdown>
    
    <Dropdown 
      size="lg"
      items={[
        { children: 'Large', onClick: () => console.log('Large') }
      ]}
    >
      <Button size="sm">Large</Button>
    </Dropdown>
  </div>
</CodeBlock>

## With Disabled Items

<CodeBlock code={`<Dropdown 
  items={[
    { children: 'Enabled', onClick: () => console.log('Enabled') },
    { children: 'Disabled', disabled: true, onClick: () => console.log('Disabled') },
    { children: 'Another Enabled', onClick: () => console.log('Another') }
  ]}
>
  <Button variant="secondary">With Disabled</Button>
</Dropdown>`}>
  <Dropdown 
    items={[
      { children: 'Enabled', onClick: () => console.log('Enabled') },
      { children: 'Disabled', disabled: true, onClick: () => console.log('Disabled') },
      { children: 'Another Enabled', onClick: () => console.log('Another') }
    ]}
  >
    <Button variant="secondary">With Disabled</Button>
  </Dropdown>
</CodeBlock>

## Manual Components

<CodeBlock code={`<DropdownMenu placement="bottom" size="md">
  <DropdownItem icon={<Icon name="user" size="sm" />}>Profile</DropdownItem>
  <DropdownItem icon={<Icon name="settings" size="sm" />}>Settings</DropdownItem>
  <DropdownDivider />
  <DropdownItem icon={<Icon name="close" size="sm" />}>Logout</DropdownItem>
</DropdownMenu>`}>
  <DropdownMenu placement="bottom" size="md">
    <DropdownItem icon={<Icon name="user" size="sm" />}>Profile</DropdownItem>
    <DropdownItem icon={<Icon name="settings" size="sm" />}>Settings</DropdownItem>
    <DropdownDivider />
    <DropdownItem icon={<Icon name="close" size="sm" />}>Logout</DropdownItem>
  </DropdownMenu>
</CodeBlock>

## Props

### Dropdown Props

<PropsTable props={[
  { name: 'children', type: 'React.ReactNode', default: '-', description: 'The trigger element' },
  { name: 'items', type: 'DropdownItemProps[]', default: '-', description: 'Array of dropdown items' },
  { name: 'placement', type: "'top' | 'bottom' | 'left' | 'right'", default: "'bottom'", description: 'Placement of the dropdown' },
  { name: 'size', type: "'sm' | 'md' | 'lg'", default: "'md'", description: 'Size variant' },
  { name: 'isOpen', type: 'boolean', default: '-', description: 'Whether the dropdown is open (controlled)' },
  { name: 'onOpenChange', type: '(isOpen: boolean) => void', default: '-', description: 'Callback when dropdown open state changes' },
  { name: 'className', type: 'string', default: '-', description: 'Additional CSS classes' }
]} />

### DropdownItem Props

<PropsTable props={[
  { name: 'children', type: 'React.ReactNode', default: '-', description: 'Item content' },
  { name: 'icon', type: 'React.ReactNode', default: '-', description: 'Icon to display before the text' },
  { name: 'disabled', type: 'boolean', default: 'false', description: 'Whether the item is disabled' },
  { name: 'onClick', type: '() => void', default: '-', description: 'Click handler' },
  { name: 'className', type: 'string', default: '-', description: 'Additional CSS classes' }
]} />
