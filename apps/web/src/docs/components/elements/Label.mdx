import { Label } from '@losensky-systems/web-components-core'

# Label Component

The Label component provides accessible labels for form elements with proper contrast and styling.

## Import

```tsx
import { Label } from '@losensky-systems/web-components-core'
```

## Basic Usage

<div style={{ marginBottom: '2rem' }}>
  <Label htmlFor="email">Email Address</Label>
  <Label htmlFor="password">Password</Label>
  <Label htmlFor="name" required>Full Name</Label>
</div>

```tsx
<Label htmlFor="email">Email Address</Label>
<Label htmlFor="password">Password</Label>
<Label htmlFor="name" required>Full Name</Label>
```

## Sizes

<div style={{ marginBottom: '2rem' }}>
  <Label size="sm" htmlFor="small">Small Label</Label>
  <Label size="md" htmlFor="medium">Medium Label (Default)</Label>
  <Label size="lg" htmlFor="large">Large Label</Label>
</div>

```tsx
<Label size="sm" htmlFor="small">Small Label</Label>
<Label size="md" htmlFor="medium">Medium Label (Default)</Label>
<Label size="lg" htmlFor="large">Large Label</Label>
```

## Colors

<div style={{ marginBottom: '2rem' }}>
  <Label color="default" htmlFor="default">Default Label</Label>
  <Label color="primary" htmlFor="primary">Primary Label</Label>
  <Label color="success" htmlFor="success">Success Label</Label>
  <Label color="warning" htmlFor="warning">Warning Label</Label>
  <Label color="error" htmlFor="error">Error Label</Label>
  <Label color="muted" htmlFor="muted">Muted Label</Label>
</div>

```tsx
<Label color="default" htmlFor="default">Default Label</Label>
<Label color="primary" htmlFor="primary">Primary Label</Label>
<Label color="success" htmlFor="success">Success Label</Label>
<Label color="warning" htmlFor="warning">Warning Label</Label>
<Label color="error" htmlFor="error">Error Label</Label>
<Label color="muted" htmlFor="muted">Muted Label</Label>
```

## Helper Text

<div style={{ marginBottom: '2rem' }}>
  <Label htmlFor="username" helperText="Choose a unique username">
    Username
  </Label>
  <Label htmlFor="password" errorText="Password must be at least 8 characters">
    Password
  </Label>
  <Label htmlFor="email" successText="Email address is valid">
    Email Address
  </Label>
</div>

```tsx
<Label htmlFor="username" helperText="Choose a unique username">
  Username
</Label>
<Label htmlFor="password" errorText="Password must be at least 8 characters">
  Password
</Label>
<Label htmlFor="email" successText="Email address is valid">
  Email Address
</Label>
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `children` | `React.ReactNode` | - | Label text content |
| `htmlFor` | `string` | - | ID of associated form element |
| `required` | `boolean` | `false` | Show required asterisk |
| `disabled` | `boolean` | `false` | Disabled state |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Label size |
| `color` | `'default' \| 'muted' \| 'primary' \| 'success' \| 'warning' \| 'error' \| 'info'` | `'default'` | Label color |
| `helperText` | `string` | - | Helper text below label |
| `errorText` | `string` | - | Error text below label |
| `successText` | `string` | - | Success text below label |
| `className` | `string` | `''` | Additional CSS classes |
