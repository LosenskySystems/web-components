import { Divider } from '@losensky-systems/web-components-core'

# Divider Component

The Divider component creates visual separation between content sections. It supports horizontal and vertical orientations, different styles, and can include labels.

## Import

```tsx
import { Divider } from '@losensky-systems/web-components-core'
```

## Basic Usage

Simple dividers for content separation:

<CodeBlock code={`<p>Content above the divider</p>
<Divider />
<p>Content below the divider</p>`}>
  <div className="mb-8">
    <p>Content above the divider</p>
    <Divider />
    <p>Content below the divider</p>
  </div>
</CodeBlock>

## Orientations

### Horizontal (Default)
Horizontal dividers separate content vertically:

<CodeBlock code={`<div className="p-4 bg-gray-50 rounded-lg mb-4">
  Section 1
</div>
<Divider />
<div className="p-4 bg-gray-50 rounded-lg mt-4">
  Section 2
</div>`}>
  <div className="mb-8">
    <div className="p-4 bg-gray-50 rounded-lg mb-4">
      Section 1
    </div>
    <Divider />
    <div className="p-4 bg-gray-50 rounded-lg mt-4">
      Section 2
    </div>
  </div>
</CodeBlock>

### Vertical
Vertical dividers separate content horizontally:

<CodeBlock code={`<div className="flex items-center h-16">
  <div className="px-4 py-2 bg-gray-50 rounded-lg">
    Left Content
  </div>
  <Divider orientation="vertical" />
  <div className="px-4 py-2 bg-gray-50 rounded-lg">
    Right Content
  </div>
</div>`}>
  <div className="flex items-center h-16 mb-8">
    <div className="px-4 py-2 bg-gray-50 rounded-lg">
      Left Content
    </div>
    <Divider orientation="vertical" />
    <div className="px-4 py-2 bg-gray-50 rounded-lg">
      Right Content
    </div>
  </div>
</CodeBlock>

## Variants

Different border styles for visual variety:

<CodeBlock code={`<p>Solid divider (default):</p>
<Divider variant="solid" />

<p className="mt-4">Dashed divider:</p>
<Divider variant="dashed" />

<p className="mt-4">Dotted divider:</p>
<Divider variant="dotted" />`}>
  <div className="mb-8">
    <p>Solid divider (default):</p>
    <Divider variant="solid" />
    
    <p className="mt-4">Dashed divider:</p>
    <Divider variant="dashed" />
    
    <p className="mt-4">Dotted divider:</p>
    <Divider variant="dotted" />
  </div>
</CodeBlock>

## Spacing

Control the margin around dividers:

<CodeBlock code={`<p>Small spacing:</p>
<Divider spacing="sm" />
<p>Medium spacing (default):</p>
<Divider spacing="md" />
<p>Large spacing:</p>
<Divider spacing="lg" />
<p>Extra large spacing:</p>
<Divider spacing="xl" />`}>
  <div className="mb-8">
    <p>Small spacing:</p>
    <Divider spacing="sm" />
    <p>Medium spacing (default):</p>
    <Divider spacing="md" />
    <p>Large spacing:</p>
    <Divider spacing="lg" />
    <p>Extra large spacing:</p>
    <Divider spacing="xl" />
  </div>
</CodeBlock>

## Colors

Use semantic colors for different contexts:

<CodeBlock code={`<p>Default divider:</p>
<Divider color="default" />

<p className="mt-4">Primary divider:</p>
<Divider color="primary" />

<p className="mt-4">Success divider:</p>
<Divider color="success" />

<p className="mt-4">Warning divider:</p>
<Divider color="warning" />

<p className="mt-4">Error divider:</p>
<Divider color="error" />

<p className="mt-4">Info divider:</p>
<Divider color="info" />`}>
  <div className="mb-8">
    <p>Default divider:</p>
    <Divider color="default" />
    
    <p className="mt-4">Primary divider:</p>
    <Divider color="primary" />
    
    <p className="mt-4">Success divider:</p>
    <Divider color="success" />
    
    <p className="mt-4">Warning divider:</p>
    <Divider color="warning" />
    
    <p className="mt-4">Error divider:</p>
    <Divider color="error" />
    
    <p className="mt-4">Info divider:</p>
    <Divider color="info" />
  </div>
</CodeBlock>

## Labeled Dividers

Add text labels to dividers for better context:

<CodeBlock code={`<Divider>OR</Divider>

<Divider color="primary">Section Break</Divider>

<Divider color="success">Success Message</Divider>

<Divider color="warning">Important Notice</Divider>`}>
  <div className="mb-8">
    <Divider>OR</Divider>
    
    <Divider color="primary">Section Break</Divider>
    
    <Divider color="success">Success Message</Divider>
    
    <Divider color="warning">Important Notice</Divider>
  </div>
</CodeBlock>

## Real-World Examples

Common use cases for dividers:

<CodeBlock code={`<div className="p-4 bg-gray-50 rounded-lg">
  <h3 className="text-lg font-semibold mb-2">User Profile</h3>
  <p className="m-0">Personal information and settings</p>
</div>

<Divider spacing="lg" />

<div className="p-4 bg-gray-50 rounded-lg">
  <h3 className="text-lg font-semibold mb-2">Account Settings</h3>
  <p className="m-0">Security and privacy options</p>
</div>

<Divider spacing="lg" />

<div className="p-4 bg-gray-50 rounded-lg">
  <h3 className="text-lg font-semibold mb-2">Billing</h3>
  <p className="m-0">Payment methods and subscription</p>
</div>`}>
  <div className="mb-8">
    <div className="p-4 bg-gray-50 rounded-lg">
      <h3 className="text-lg font-semibold mb-2">User Profile</h3>
      <p className="m-0">Personal information and settings</p>
    </div>
    
    <Divider spacing="lg" />
    
    <div className="p-4 bg-gray-50 rounded-lg">
      <h3 className="text-lg font-semibold mb-2">Account Settings</h3>
      <p className="m-0">Security and privacy options</p>
    </div>
    
    <Divider spacing="lg" />
    
    <div className="p-4 bg-gray-50 rounded-lg">
      <h3 className="text-lg font-semibold mb-2">Billing</h3>
      <p className="m-0">Payment methods and subscription</p>
    </div>
  </div>
</CodeBlock>

### Navigation Menu Example

<CodeBlock code={`<div className="flex items-center p-2 bg-gray-50 rounded-lg">
  <span className="px-4 py-2">Home</span>
  <Divider orientation="vertical" spacing="sm" />
  <span className="px-4 py-2">Products</span>
  <Divider orientation="vertical" spacing="sm" />
  <span className="px-4 py-2">About</span>
  <Divider orientation="vertical" spacing="sm" />
  <span className="px-4 py-2">Contact</span>
</div>`}>
  <div className="flex items-center p-2 bg-gray-50 rounded-lg mb-8">
    <span className="px-4 py-2">Home</span>
    <Divider orientation="vertical" spacing="sm" />
    <span className="px-4 py-2">Products</span>
    <Divider orientation="vertical" spacing="sm" />
    <span className="px-4 py-2">About</span>
    <Divider orientation="vertical" spacing="sm" />
    <span className="px-4 py-2">Contact</span>
  </div>
</CodeBlock>

### Form Section Example

<CodeBlock code={`<div className="p-4 bg-gray-50 rounded-lg">
  <h4 className="text-lg font-semibold mb-2">Personal Information</h4>
  <p className="m-0 text-sm text-gray-600">Enter your basic details</p>
</div>

<Divider color="primary" spacing="md" />

<div className="p-4 bg-gray-50 rounded-lg">
  <h4 className="text-lg font-semibold mb-2">Contact Details</h4>
  <p className="m-0 text-sm text-gray-600">Phone and email information</p>
</div>`}>
  <div className="mb-8">
    <div className="p-4 bg-gray-50 rounded-lg">
      <h4 className="text-lg font-semibold mb-2">Personal Information</h4>
      <p className="m-0 text-sm text-gray-600">Enter your basic details</p>
    </div>
    
    <Divider color="primary" spacing="md" />
    
    <div className="p-4 bg-gray-50 rounded-lg">
      <h4 className="text-lg font-semibold mb-2">Contact Details</h4>
      <p className="m-0 text-sm text-gray-600">Phone and email information</p>
    </div>
  </div>
</CodeBlock>

## Accessibility

The Divider component includes proper accessibility features:

- **ARIA attributes**: `role="separator"` and `aria-orientation`
- **Semantic meaning**: Clear indication of content separation
- **Screen reader friendly**: Proper announcements for content structure
- **Keyboard accessible**: Works with focus management

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `orientation` | `'horizontal' \| 'vertical'` | `'horizontal'` | Divider direction |
| `variant` | `'solid' \| 'dashed' \| 'dotted'` | `'solid'` | Border style |
| `spacing` | `'sm' \| 'md' \| 'lg' \| 'xl'` | `'md'` | Margin around divider |
| `color` | `'default' \| 'primary' \| 'success' \| 'warning' \| 'error' \| 'info'` | `'default'` | Divider color |
| `className` | `string` | `''` | Additional CSS classes |
| `children` | `React.ReactNode` | - | Label text for labeled dividers |